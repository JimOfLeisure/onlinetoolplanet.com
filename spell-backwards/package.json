{
  "name": "spell-backwards",
  "version": "1.0.0",
  "description": "",
  "main": "spell-backwards.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "mkdir dist || true; npx tsc --target es2015 src/spellbackwards.ts --outDir dist && esbuild dist/spellbackwards.js --bundle --outfile=dist/bundle.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "esrever": "^0.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.12.24",
    "typescript": "^4.4.2"
  }
}
